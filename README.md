# dotfiles

A place to store my configuration files.

USAGE: Run `./setup.sh <module to link>`

## Migrating from ~/bin

If upgrading from an older version, run `./migrate-to-local-bin.sh` first.

## Scripts

Links commonly used scripts to `~/.local/bin/`
