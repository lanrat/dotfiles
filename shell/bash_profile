# bash_profile is executed for login shells (login and ssh)

if [ -f ~/.profile ]; then
   source ~/.profile
fi

if [ -f ~/.bashrc ]; then
   source ~/.bashrc
fi
export BASH_SILENCE_DEPRECATION_WARNING=1

