# ~/.profile: executed by the command interpreter for login shells (non-gui).
# This file is not read by bash(1), if ~/.bash_profile or ~/.bash_login
# exists.

# sbin PATH
if [ -d "/sbin" ] ; then
    PATH="/sbin:$PATH"
fi
if [ -d "/usr/sbin" ] ; then
    PATH="/usr/sbin:$PATH"
fi
# set PATH so it includes user's private bin if it exists
if [ -d "$HOME/bin" ] ; then
    PATH="$HOME/bin:$PATH"
fi

# disable Software flow control
# AKA: Ctrl-s freezing term until ctrl-q is pressed
#stty -ixon

#disable caps-lock
#setxkbmap -option ctrl:nocaps
# to re-enable: setxkbmap -optio

# Include local machiene specific settings
if [ -f ~/.profile_local ]; then
    . ~/.profile_local
fi
